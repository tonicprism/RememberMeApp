import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#fff',
    borderWidth: 2,
    borderColor: '#fff',
    borderRadius: 20,
    textAlign: 'center',
    minWidth: '100%',
  },

  button: {},
});

export default styles;
